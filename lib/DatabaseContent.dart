class DatabaseContent {
  late String dataflow, last_update, freq, unit, coicop, geo, time_period, obs_value,;
  String? obs_flag;
  DatabaseContent();

  DatabaseContent.fromMap({Map<String, dynamic>} res): dataflow
}